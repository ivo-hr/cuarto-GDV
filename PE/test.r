# Simple R script to test the environment

# Print a message
print("Hello, World!")

# Perform a basic calculation
result <- 2 + 2
print(paste("2 + 2 =", result))

# Create a simple plot
x <- c(1, 2, 3, 4, 5)
y <- c(2, 4, 6, 8, 10)
plot(x, y, main="Simple Plot", xlab="X-axis", ylab="Y-axis", col="blue", pch=19)